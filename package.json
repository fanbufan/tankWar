{
  "name": "tank-war",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "assets:generate": "node scripts/generate-sprites.mjs",
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^5.9.3",
    "vite": "^7.2.4",
    "vitest": "^4.0.14"
  }
}
